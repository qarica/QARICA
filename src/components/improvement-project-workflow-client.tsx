"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";const L:Record<string,string>={DRAFT:"Nháp",PENDING_APPROVAL:"Chờ phê duyệt",APPROVED:"Đã phê duyệt",IN_PROGRESS:"Đang PDSA",EVALUATED:"Đã đánh giá đạt",CLOSED:"Đã đóng"};
export function ImprovementProjectWorkflowClient({recordId,status,canManage,objectives,milestones,actions,incomplete,evidence}:{recordId:string;status:string;canManage:boolean;objectives:number;milestones:number;actions:number;incomplete:number;evidence:number}){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");const[notice,setNotice]=useState("");const[summary,setSummary]=useState("");const[result,setResult]=useState("ACHIEVED");const[sustain,setSustain]=useState(true);const[scale,setScale]=useState(false);async function cmd(action:string,payload:Record<string,unknown>={}){setBusy(true);setError("");try{const r=await fetch(`/api/improvement/projects/${recordId}/workflow`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action,...payload})});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error);setNotice(j.message);router.refresh()}catch(e){setError(e instanceof Error?e.message:"Không xử lý được đề án.")}finally{setBusy(false)}}function evaluate(e:FormEvent){e.preventDefault();cmd("EVALUATE",{objective_achievement_summary:summary,overall_result:result,sustainability_required:sustain,scaleout_recommended:scale})}return <section className="panel"><div style={{padding:18,display:"flex",justifyContent:"space-between",gap:12}}><div><h2 style={{margin:0,fontSize:18}}>Improvement Project · SMART & PDSA</h2><p style={{color:"#64748b",fontSize:12}}>Baseline → mục tiêu SMART → milestone/PDSA → Action → dữ liệu trước-sau → sustain/spread.</p></div><strong>{L[status]||status}</strong></div><div style={{padding:"0 18px 18px",display:"grid",gap:12}}>{error?<div className="alert error">{error}</div>:null}{notice?<div className="alert success">{notice}</div>:null}<div className="domain-metrics"><div><strong>{objectives}</strong><span>Mục tiêu SMART</span></div><div><strong>{milestones}</strong><span>Milestone/PDSA</span></div><div><strong>{actions}/{incomplete}</strong><span>Action / chưa xong</span></div><div><strong>{evidence}</strong><span>Minh chứng</span></div></div>{status==="DRAFT"&&canManage?<button className="button primary" disabled={busy||objectives<1||milestones<1} onClick={()=>cmd("SUBMIT")}>Gửi phê duyệt</button>:null}{status==="PENDING_APPROVAL"&&canManage?<button className="button primary" disabled={busy} onClick={()=>cmd("APPROVE")}>Phê duyệt đề án</button>:null}{status==="APPROVED"&&canManage?<button className="button primary" disabled={busy} onClick={()=>cmd("START")}>Bắt đầu triển khai PDSA</button>:null}{status==="IN_PROGRESS"&&canManage?<form onSubmit={evaluate} className="domain-detail-grid"><label className="wide">Đánh giá mức đạt mục tiêu *<textarea rows={4} value={summary} onChange={e=>setSummary(e.target.value)}/></label><label>Kết quả<select value={result} onChange={e=>setResult(e.target.value)}><option value="ACHIEVED">Đạt</option><option value="PARTIAL">Đạt một phần</option><option value="NOT_ACHIEVED">Chưa đạt</option></select></label><label><input type="checkbox" checked={sustain} onChange={e=>setSustain(e.target.checked)}/> Cần kế hoạch duy trì</label><label><input type="checkbox" checked={scale} onChange={e=>setScale(e.target.checked)}/> Đề xuất nhân rộng</label><button className="button primary" disabled={busy||actions<1||incomplete>0||evidence<1}>Đánh giá kết quả</button></form>:null}{status==="EVALUATED"&&canManage?<button className="button primary" disabled={busy} onClick={()=>{const x=window.prompt("Kết luận duy trì/nhân rộng:");if(x?.trim())cmd("CLOSE",{comment:x.trim()})}}>Đóng đề án</button>:null}</div></section>}

