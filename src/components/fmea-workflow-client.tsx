"use client";import {useState} from "react";import {useRouter} from "next/navigation";const L:Record<string,string>={DRAFT:"Nháp",PENDING_APPROVAL:"Chờ phê duyệt",IN_PROGRESS:"Đang can thiệp",RESIDUAL_REVIEW:"Re-score residual risk",CLOSED:"Đã đóng"};export function FmeaWorkflowClient({recordId,status,canManage,hasModel,steps,modes,highPriority,actions,incomplete,evidence}:{recordId:string;status:string;canManage:boolean;hasModel:boolean;steps:number;modes:number;highPriority:number;actions:number;incomplete:number;evidence:number}){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");const[notice,setNotice]=useState("");async function cmd(action:string,payload:Record<string,unknown>={}){setBusy(true);setError("");try{const r=await fetch(`/api/fmea/${recordId}/workflow`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action,...payload})});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error);setNotice(j.message);router.refresh()}catch(e){setError(e instanceof Error?e.message:"Không xử lý được FMEA.")}finally{setBusy(false)}}return <section className="panel"><div className="panel-title"><div><h2>FMEA/HFMEA Workflow</h2><p>Quy trình → failure mode → ưu tiên → Action → re-score → residual risk.</p></div><strong>{L[status]||status}</strong></div><div style={{padding:"0 18px 18px",display:"grid",gap:12}}>{error?<div className="alert error">{error}</div>:null}{notice?<div className="alert success">{notice}</div>:null}<div className="domain-metrics"><div><strong>{hasModel?"Có":"Thiếu"}</strong><span>Mô hình điểm</span></div><div><strong>{steps}</strong><span>Bước quy trình</span></div><div><strong>{modes}</strong><span>Failure mode</span></div><div><strong>{highPriority}</strong><span>Ưu tiên cao</span></div><div><strong>{actions}/{incomplete}</strong><span>Action/chưa xong</span></div><div><strong>{evidence}</strong><span>Minh chứng</span></div></div>{status==="DRAFT"&&canManage?<button className="button primary" disabled={busy||!hasModel||steps<1||modes<1} onClick={()=>cmd("SUBMIT")}>Gửi phê duyệt FMEA</button>:null}{status==="PENDING_APPROVAL"&&canManage?<button className="button primary" disabled={busy} onClick={()=>cmd("APPROVE")}>Phê duyệt triển khai</button>:null}{status==="IN_PROGRESS"&&canManage?<button className="button primary" disabled={busy||(highPriority>0&&actions<1)||incomplete>0||evidence<1} onClick={()=>cmd("REQUEST_RESIDUAL_REVIEW")}>Chuyển re-score residual risk</button>:null}{status==="RESIDUAL_REVIEW"&&canManage?<button className="button primary" disabled={busy} onClick={()=>{const x=window.prompt("Kết luận re-score và chấp nhận residual risk:");if(x?.trim())cmd("CLOSE",{comment:x.trim()})}}>Chấp nhận residual risk & đóng</button>:null}</div></section>}
