"use client";
import { FormEvent,useState } from "react";import { useRouter,useSearchParams } from "next/navigation";import { Eye,EyeOff,LoaderCircle } from "lucide-react";import { createClient } from "@/lib/supabase/client";
const INTERNAL_LOGIN_DOMAIN="qlcl-ttsg.local";
function toAuthEmail(login:string){const v=login.trim().toLowerCase();return v.includes("@")?v:`${v}@${INTERNAL_LOGIN_DOMAIN}`}
export function LoginForm(){const router=useRouter();const search=useSearchParams();const [login,setLogin]=useState("");const [password,setPassword]=useState("");const [showPassword,setShowPassword]=useState(false);const [loading,setLoading]=useState(false);const [error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");setLoading(true);const supabase=createClient();const {error}=await supabase.auth.signInWithPassword({email:toAuthEmail(login),password});if(error){setError("Tài khoản hoặc mật khẩu không đúng, hoặc tài khoản chưa được kích hoạt.");setLoading(false);return}router.replace(search.get("next")||"/dashboard");router.refresh()}return <form className="login-card" onSubmit={submit}><div className="mobile-brand"><div className="brand-mark">QL</div><strong>QLCL-TTSG</strong></div><div><div className="eyebrow">ĐĂNG NHẬP HỆ THỐNG</div><h2>Chào mừng trở lại</h2><p className="muted">Sử dụng tài khoản và mật khẩu do quản trị viên cấp. Không bắt buộc email.</p></div><label><span>Tài khoản</span><input type="text" value={login} onChange={e=>setLogin(e.target.value)} placeholder="Ví dụ: khoa.noi hoặc qlcl01" autoComplete="username" required/></label><label><span>Mật khẩu</span><div className="password-wrap"><input type={showPassword?"text":"password"} value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••••••" autoComplete="current-password" required/><button type="button" className="icon-button ghost" onClick={()=>setShowPassword(v=>!v)} aria-label="Hiện/ẩn mật khẩu">{showPassword?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label>{error?<div className="alert error">{error}</div>:null}<button className="button primary wide" type="submit" disabled={loading}>{loading?<><LoaderCircle className="spin" size={18}/> Đang đăng nhập...</>:"Đăng nhập"}</button><p className="tiny muted center">Dữ liệu truy cập được kiểm soát theo vai trò, quyền và phạm vi khoa/phòng.</p></form>}
