import {DomainRecordPage} from "@/components/domain-record-page";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;return <DomainRecordPage id={id} moduleTitle="Đề án cải tiến" listHref="/improvement/projects" permissions={["projects.view","projects.propose","projects.manage"]}/>}