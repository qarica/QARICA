import { AdminDepartmentsClient } from "@/components/admin-departments-client";import { PageHeader } from "@/components/page-header";import { requirePermission,requireUserContext } from "@/lib/auth";import { createClient } from "@/lib/supabase/server";
export default async function DepartmentsPage(){const {user}=await requireUserContext();requirePermission(user,"departments.manage");const supabase=await createClient();const {data,error}=await supabase.from("departments").select("id,code,name,short_name,department_type,parent_department_id,is_active").order("name");return <div className="page-stack"><PageHeader eyebrow="QUẢN TRỊ" title="Khoa / Phòng" description="Thêm, đổi tên hoặc ngưng hoạt động đơn vị mà không sửa code. Đơn vị đã có lịch sử nên ngưng hoạt động thay vì xóa."/>{error?<div className="alert error">Không tải được danh mục: {error.message}</div>:null}<AdminDepartmentsClient departments={(data??[]) as any[]}/></div>}
