import { AdminSettingsClient } from "@/components/admin-settings-client";import { PageHeader } from "@/components/page-header";import { requirePermission,requireUserContext } from "@/lib/auth";import { createClient } from "@/lib/supabase/server";
export default async function SettingsPage(){const {user}=await requireUserContext();requirePermission(user,"system.manage");const supabase=await createClient();const {data,error}=await supabase.from("organizations").select("id,name,short_name,code,address,website,timezone,primary_color,secondary_color,record_code_prefix,is_active").eq("id",user.organizationId).maybeSingle();return <div className="page-stack"><PageHeader eyebrow="QUẢN TRỊ" title="Cấu hình hệ thống" description="Thông tin tổ chức và nhận diện lưu trong database để cùng codebase có thể triển khai cho bệnh viện khác."/>{error?<div className="alert error">Không tải được cấu hình: {error.message}</div>:null}{data?<AdminSettingsClient organization={data as any}/>:<div className="empty-state panel">Tài khoản hiện chưa gắn với một tổ chức.</div>}</div>}
