import {DomainRecordPage} from "@/components/domain-record-page";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;return <DomainRecordPage id={id} moduleTitle="Phản ánh / Góp ý" listHref="/feedback" permissions={["feedback.view","feedback.manage"]}/>}